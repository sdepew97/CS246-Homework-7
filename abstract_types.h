/* abstract_types.h

   Defines the mutually recursive movie and cast_member types
*/

#ifndef ABSTRACT_TYPES_H_
#define ABSTRACT_TYPES_H_

// these abstract types are used in the collections, so they must
// be declared first
typedef struct cast_member cast_member;
typedef struct movie movie;

#endif
